{
  "name": "digital-image-processing",
  "version": "0.1.0",
  "description": "Experimentos de Processamento Digital de Imagens",
  "private": true,
  "scripts": {
    "hooks:install": "bash scripts/git-hooks/install.sh",
    "hooks:install:win": "pwsh -File scripts/git-hooks/install.ps1",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
